# Backend API URL
# Use relative path to leverage Vite's proxy configuration
VITE_API_URL=/api/v1

# Note: The Vite dev server will proxy /api requests to http://localhost:3000
# This allows you to expose only the frontend port via ngrok
